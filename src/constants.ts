// Movement constants - shared between player and pushable entities
// to ensure synchronized movement animations
export const MOVE_SPEED = 4; // tiles per second

// Fall animation constants
export const FALL_SPEED = 1.5; // duration in seconds for fall animation
